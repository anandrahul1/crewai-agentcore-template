name: "{{AGENT_NAME}}"
description: "CrewAI agent for {{TEAM_NAME}} team"
entrypoint: "src/agent.py"
runtime:
  memory: 512
  timeout: 300
environment:
  AWS_REGION: "us-west-2"
  TEAM_NAME: "{{TEAM_NAME}}"
  MODEL_ID: "{{MODEL_ID}}"
